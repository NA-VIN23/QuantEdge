// src/__tests__/setup.ts
// Use the Vitest-specific subpath so @testing-library/jest-dom augments
// Vitest's Assertion<T> type rather than Jest's expect type.
import '@testing-library/jest-dom/vitest';
